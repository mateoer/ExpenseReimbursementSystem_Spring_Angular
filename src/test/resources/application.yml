

#this is a comment in a yml file (yaml)


server:
  port: 9050
  
spring:
  datasource:
#    url: jdbc:postgresql://${TRAINING_DB_ENDPOINT}/foodDB
    url: jdbc:h2:file:./myTestH2Folder/myTestH2file
    username: sa
    password: mypass
    driver-class-name: org.h2.Driver
  sql:
    init:
      data-locations:
#      - classpath:./testdata.sql
#      schema-locations:
#      - classpath:./myschema.sql
      mode: always
  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: false
    hibernate:
      ddl-auto: create-drop
#    properties:
#      hibernate:
#        temp:
#          use_jdbc_metadata_defaults: false
#        jdbc:
#          lob:
#            non_contextual_creation: true
 
 
  
